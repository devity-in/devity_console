import 'package:flutter/material.dart';

class DataFactoryScreen extends StatelessWidget {
  const DataFactoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Data Factory'),
        automaticallyImplyLeading: false,
      ),
      body: const Center(
        child: Text('Data Factory Screen - Content Coming Soon!'),
      ),
    );
  }
}
