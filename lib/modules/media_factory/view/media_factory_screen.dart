import 'package:flutter/material.dart';

class MediaFactoryScreen extends StatelessWidget {
  const MediaFactoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Media Factory'),
        automaticallyImplyLeading: false,
      ),
      body: const Center(
        child: Text('Media Factory Screen - Content Coming Soon!'),
      ),
    );
  }
}
