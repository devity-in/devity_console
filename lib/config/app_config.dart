/// Application configuration constants.
class AppConfig {
  /// Base URL for the Devity backend API.
  /// TODO: Make this configurable via environment variables (.env file).
  static const String backendBaseUrl = 'http://localhost:8001';
}
